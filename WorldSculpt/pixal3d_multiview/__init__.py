"""Multi-view extension package for Pixal3D.

Holds the pieces the released inference pipeline uses: LoRA wiring (`lora`), the
view aggregators (`mv_aggregator`), projection helpers (`utils`), and the mixin the
per-view conditioning lives in (`trainers`).
"""
